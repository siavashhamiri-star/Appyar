# Data Layer
Contains repository implementations, data sources (local & remote), and data mappers.
- `datasource/` (local database, remote api)
- `repository/` (repository implementations)
- `mapper/` (data entity to domain model mappers)
